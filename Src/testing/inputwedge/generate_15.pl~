#!/usr/bin/perl
use strict;
open(my $$IN,"<ligands3.dat")|| die;
my @ligs=();
while(my $line=<$IN>){
    chomp($line);
    push(@$ligs,$line);
    
}
    
for my $i (0..8191){
    for my $k (0..$#ligs){
	print ",$i $ligs[$k]\n";
    }
}
